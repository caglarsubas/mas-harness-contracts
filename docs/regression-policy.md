# CON-FIX-001 cumulative regression policy

Phase: Foundation correction, before Alpha-2 model contracts.
Status at this draft checkpoint: SOURCE_PREPARED; signed acceptance is
NOT_RUN_ENV_UNAVAILABLE. This packet is not complete and must not be merged.

## Authority and baseline

The approved packet is CON-FIX-001, amended by merged MET-REPAIR-002 (meta
PR 93, f753b99eeeaf74b57270ab3b4fe9884cab424c37). Its YAML SHA-256 is
15040a4811277880118d58121a7d23721d8a91b4ab6d3211f960189d6a351a14.
The old packet signature does not authorize this amended scope.

The immutable CON-007 baseline is contracts main
2146278a95344cd2a8e22596b2f315b46edffc88. The historical signed replay returned
181 passed, three failed, zero skipped; log SHA-256
8a918152e335c0d2c48fac7072b70bc3a69acea9caaf4c4a03e9a33d939f74ec.
Those failures are not relabelled as fixed or replaced by a filtered test run.

`contracts/regression-inputs/con-007-release-manifest.json` is a byte-identical
copy of this product repository's predecessor manifest, not warm-source content.
`con-007-baseline.json` binds that manifest, the historical failed replay,
59 raw file digests, and the exact implementation boundary. Both records are
digest-pinned by the golden tests. No warm-start checkout is accessed or copied.

## Prepared corrections

- The output inventory independently includes generated, compatibility and
  release outputs; missing, invented and byte-stale outputs must fail.
- API inventory comes from the actual local filesystem and retains mandatory
  predecessor APIs. Duplicated, omitted, invented and wrong-digest index entries
  fail; a valid additional local OpenAPI description is accepted in a temporary
  fixture root. The current-root check remains separate and mandatory.
- Explicit generator root parameters support real fixture trees without replacing
  the current repository or monkeypatching generator/registry behavior. Default
  command behavior and generated bytes are unchanged at this checkpoint.
- Extension lineage must be ordered and unique, retaining CON-007 before later
  entries. Tests cover legal additions and dropped/duplicated/reordered ancestors.
- The empty registry test uses an actually empty directory. The current registry
  separately requires CON-002, CON-004 and CON-006 with their exact commands;
  restricted CON-002 authority still rejects both later-owned descriptors.

## Independent status vectors

`tests/fixtures/status/aggregation-interoperability.json` is an
INDEPENDENT_CONTRACT_VECTOR fixture. Expected states are hand-authored from
`docs/status-projections.md`; they are not generated by aggregate_status.
The fixture binds the unchanged documentation and status-semantics digests.

The cross-language input profile is
COMPLETE_ELEVEN_AXIS_PASS_SNAPSHOT_WITH_EXPLICIT_OVERRIDES_V1:

1. Materialize exactly the ordered eleven `axisIds` for each harness as required
   PASS axes. Preserve the declared harness ID, selection and installation.
2. Apply only explicitly named `axisOverrides`. WAIVED retains its underlying
   non-pass state and a fixture-only digest-bound approval; NOT_APPLICABLE carries
   a fixture-only applicability contract. These are test inputs, not live approvals.
3. For the four-by-sixteen selection/installation matrix, use passing axes and
   read the expected state from its literal row. For each freshness state, read
   the literal `freshnessExpected` mapping; never call an implementation for an
   expected value.
4. Exercise each of the nine evidence states as required and optional on every
   axis; exercise every permitted waiver underlying state in both roles. Scenario
   rows additionally define failure/revocation precedence, selection neutrality,
   explanation counts, lexical contributor ordering and outage behavior.
5. Compare each declared expected field exactly and assert that aggregation did
   not mutate the input. Repeat multi-harness permutations without event-order input.

The named blocked-selection/degraded-installation regression expects BLOCKED
with CURRENT freshness and passing axes. It must fail under the unchanged
predecessor implementation before the approved correction is made. There is no
skip, xfail, deselection or mock that converts this pending regression to success.

## Remaining work — do not claim completion

| ID / gate | Status | Required next work |
|---|---|---|
| CON-FIX-001 regression source | PREPARED, unexecuted | Review and run all three declared commands in one signed offline session |
| Runner custody | NOT_RUN_ENV_UNAVAILABLE | Install amended packet authority through a properly authorized non-interactive root-owned mechanism; no administrator password in files or commands |
| Independent failing status vector | WAITING | Record actual result and full-suite log digest under amended signed authority |
| aggregate_status correction | WAITING | Only after reproduction, make blocked selection precede degraded installation; keep REVOKED and FAILED higher |
| Implementation-change evidence | WAITING | Record before/after SHA-256, exact MODEL_IMPLEMENTATION exception and real failing-vector evidence |
| Vector release binding | WAITING | Add vector digest to generator/manifest as INDEPENDENT_CONTRACT_VECTOR and append CON-FIX-001 to unique ordered lineage |
| Local offline / PR CI / merge / exact-main | WAITING | Full acceptance, ephemeral self-hosted check, guarded merge and separate exact-main replay |
| Artifact / deployment / runtime / assurance / tenant acceptance | NOT CLAIMED | Separate future gates |

The implementation is deliberately unchanged in this draft. Every definition
outside aggregate_status, including classify_freshness, lifecycle transitions
and enums, must retain identical bytes. The golden test pins prefix/suffix
digests and requires an implementation-change record if the module changes.
The generated contract index and release manifest are derived evidence, not an
exception allowing predecessor wire or compatibility bytes to change.

Only the external signed host launcher may invoke the exact packet wrapper and
three declared acceptance argv. Do not run a targeted test or a generator command
outside that process tree. No package synchronization, downloads, hosted runners,
paid APIs, external telemetry, source imports or runtime execution are authorized.
The user has standing runner-reauthorization authority and requested no further
administrator prompt-completion requests. An unavailable non-interactive host
mechanism is a technical execution blocker, not missing user permission.

Future generator extensions must run the entire contracts suite, not just the
new packet's tests. If a regression falls outside packet paths or changes public
semantics, preserve its evidence and obtain an owning amendment before fixing it.
Revert an unconsumed corrective release as a unit; if already consumed, publish
a reviewed successor and retain the predecessor evidence rather than rewriting it.
